<h1>Search Results</h1>

<div class="span8">
    <% if @books.any? %>
      <h3>Books (<%= @books.count %>)</h3>
      <ol class="books">
        <%= render @books %>
      </ol>
      <%#= will_paginate @books %>
    <% else %>
      <h5>Sorry, no results were found</h5>      
    <% end %>
</div>